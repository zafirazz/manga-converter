import React from 'react';
import Layout from './Layout';
import '../styles/mainStyles.css';

const MainPage = () => {
    return (
        <Layout>
            <h1>Welcome to MANGAzip</h1>
            <p>Your go-to solution for manga file uploads and usage guides.</p>
        </Layout>
    );
};

export default MainPage;